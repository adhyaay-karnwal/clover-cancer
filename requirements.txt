# Core ML
torch>=2.1.0
transformers>=4.45.0
datasets>=2.16.0
trl>=0.7.0
peft>=0.7.0
accelerate>=0.25.0
gradio>=4.0.0
pyyaml>=6.0
tqdm>=4.65.0

# Optional: CUDA-only dependencies (for training on NVIDIA GPU)
# unsloth @ git+https://github.com/unslothai/unsloth.git
# bitsandbytes>=0.43.0
